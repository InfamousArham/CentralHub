# Pufferfish Configuration
# Check out Pufferfish Host for maximum performance server hosting: https://pufferfish.host
# Join our Discord for support: https://discord.gg/reZw4vQV9H
# Download new builds at https://ci.pufferfish.host/job/Pufferfish
info:
  version: '1.0'
allow-end-crystal-respawn: false
sentry-dsn: ''
enable-suffocation-optimization: false
# Optimizes entity brains when
# they're far away from the player
dab:
  enabled: true
  start-distance: 4
  max-tick-freq: 20
  activation-dist-mod: 7
  blacklisted-entities: []
inactive-goal-selector-throttle: false
# These values define a entity's maximum lifespan. If an
# entity is in this list and it has survived for longer than
# that number of ticks, then it will be removed. Setting a value to
# -1 disables this feature.
entity_timeouts:
  SNOWBALL: -1
  LLAMA_SPIT: -1
enable-books: false
# Optimizes projectile settings
projectile:
  max-loads-per-tick: 2
  max-loads-per-projectile: 2
enable-async-mob-spawning: true
# Settings for things that don't belong elsewhere
misc:
  disable-method-profiler: true
  disable-out-of-order-chat: true
  suppress-null-id-disconnections: true
tps-catchup: true
